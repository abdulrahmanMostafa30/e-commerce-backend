import express from 'express';
const router = express.Router();
// const authController = require("../../controllers/authController");
// const returnsRefundsController = require("../../controllers/returnsRefundsController");

// router.get("/", authController.restrictTo("superAdmin", "orderAdmin"), returnsRefundsController.getReturnsRefunds);
// router.put("/:id", authController.restrictTo("superAdmin", "orderAdmin"), returnsRefundsController.updateReturnRefundStatus);

// Add more routes for returns and refunds

export default router;
